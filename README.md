
  # Site sobre bioplásticos

  This is a code bundle for Site sobre bioplásticos. 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
  npm audit fix --force

  npm approve-scripts --allow-scripts-pending